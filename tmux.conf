set -g default-terminal "screen-256color"
unbind C-b
set -g prefix C-Space
bind Space send-prefix
bind | split-window -h
bind - split-window -v
unbind v
bind v split-window -p75 'vim .'; swap-pane -D

